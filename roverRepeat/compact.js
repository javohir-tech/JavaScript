/**
 * @param {Object|Array} obj
 * @return {Object|Array}
 */
var compactObject = function (obj) {

    if (typeof obj === "object") {
        const result = {};

        obj.forEach(item => {

        });
    }

};